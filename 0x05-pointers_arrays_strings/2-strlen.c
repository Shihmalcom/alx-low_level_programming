#include "main.h"

/**
 * _strlen -  function that returns the length of a string
 * @s: length
 *
 * Return: length size
 */
int _strlen(char *s)
{
	int str = 0;

	while (len = str + (*s + str))
	{
		_putchar(len + '0');
	}
}
